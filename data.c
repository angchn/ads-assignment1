#include <stdio.h>
#include "data.h"

// Parse the given .csv line into a suburb_t struct.
suburb_t *readSuburb(char *line) {
    suburb_t suburb;
    char *token;
    

    // Tokenise the string (line) to extract individual fields.
    
}
